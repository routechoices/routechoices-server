#!/usr/bin/env bash
set -e

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
BASEDIR=$(dirname $DIR)
cd $BASEDIR

./bin/reload
./bin/restart_spot
./bin/restart_task_queue
./bin/restart_tornado
./bin/restart_sse
